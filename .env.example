# Kanban app environment template

NODE_ENV=production
PORT=3400

# Legacy file path (optional)
KANBAN_PATH=/home/xiko/clawd/kanban.md

# Sensitive values: set in deployment env, never commit real values
KANBAN_TOKEN=change-me
OPENROUTER_API_KEY=change-me

# Security defaults
CORS_ORIGINS=https://kanban.repo.box
ALLOW_QUERY_TOKEN_AUTH=false
